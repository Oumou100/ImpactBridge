import { heroImage, impactStats, missionImage, navItems, ngoServices } from "@/constants";

export const useHomeContent = () => {
  return {
    navItems,
    impactStats,
    ngoServices,
    heroImage,
    missionImage,
  };
};
